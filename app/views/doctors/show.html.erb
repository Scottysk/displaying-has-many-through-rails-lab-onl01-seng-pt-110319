<h1> <%=@doctor.name%> </h1>
<h1> <%=@doctor.department%> </h1>

<% @doctor.appointments.each do |a| %>
  <p> Date: <%= appointment_datetime.strftime("%B %d, %Y at %k:%M") %></p>
  <p> Patient: <%= link_to a.patient.name, patient_path(a.patient) %> </p>